export function AfterpayMark({ className = "h-20 w-auto" }: { className?: string }) {
  return (
    <img
      src="/brand/afterpay-accepted.png"
      alt="Afterpay accepted here"
      className={className}
    />
  );
}
